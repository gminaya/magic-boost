import React from 'react';

export interface MapMarkerProps {
  lat: number
  lng: number
}

export const MapMarker = (props: MapMarkerProps) => {

  return (
    <div>{'🔥'}</div>
  );


  return null;
};